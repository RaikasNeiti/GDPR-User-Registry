#Database configuration for testing environment
TEST_DATABASE_URL=postgresql://postgres:example@localhost:5432/gdpr_registry_test

#Vault configuration for testing environment
VAULT_ADDR=https://localhost:8200
VAULT_TOKEN=your-vault-token
VAULT_SECRET_PATH=secret/data/gdpr-app

#JWT configuration for testing environment
NODE_ENV=test
NODE_TLS_REJECT_UNAUTHORIZED=0
VAULT_TLS_INSECURE=true